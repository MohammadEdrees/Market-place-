<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <!-- Application services return ActionResult<T> (explicit result objects) so the HTTP
         semantics stay defined once, next to the business rule that produced them. -->
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\MarketWorkplace.Domain\MarketWorkplace.Domain.csproj" />
  </ItemGroup>

</Project>
